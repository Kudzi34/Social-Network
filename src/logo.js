import React from "react";

export default function Logo() {
    let logoStyle = {
        color: "white",
        fontSize: "50px",
        float: "left"
    };

    return <div style={logoStyle}>ZIMCONNECT</div>;
}
